<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:conv="clr-namespace:LexiFlow.Converters"
             x:Class="LexiFlow.Views.GrammarView"
             Title="문법">
    <ContentPage.Resources>
        <conv:StatusColorConverter x:Key="StatusColor"/>
    </ContentPage.Resources>

    <Grid>
        <Grid RowDefinitions="Auto,Auto,Auto,*"
              RowSpacing="16"
              Padding="{OnIdiom Phone='20,24,20,96', Default='40,32,40,48'}"
              HorizontalOptions="Fill"
              MaximumWidthRequest="900">
            <Grid Grid.Row="0" ColumnDefinitions="*,Auto" ColumnSpacing="16">
                <VerticalStackLayout Spacing="4">
                    <Label Text="GRAMMAR LIBRARY" Style="{StaticResource EyebrowLabel}" TextColor="{StaticResource Accent}"/>
                    <Label Text="문법 라이브러리" Style="{StaticResource Title}"/>
                    <Label Text="규칙을 예문과 함께 이해하고 오래 기억하세요." Style="{StaticResource Body}"/>
                </VerticalStackLayout>
                <Button Grid.Column="1" Text="복습 시작" HeightRequest="44" VerticalOptions="End" BackgroundColor="{StaticResource Accent}" Clicked="OnQuizClick"/>
            </Grid>

            <Picker Grid.Row="1"
                    Title="카테고리 선택"
                    ItemsSource="{Binding Categories}"
                    SelectedItem="{Binding SelectedCategory}"
                    Style="{StaticResource AppPicker}"/>

            <Grid Grid.Row="2" ColumnDefinitions="*,Auto">
                <Label Text="{Binding ResultSummary}" Style="{StaticResource MutedLabel}" VerticalOptions="Center"/>
                <ActivityIndicator Grid.Column="1" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="{StaticResource Accent}"/>
            </Grid>

            <Grid Grid.Row="3">
                <CollectionView ItemsSource="{Binding FilteredGrammars}" SelectionMode="None" IsVisible="{Binding IsNotBusy}">
                    <CollectionView.EmptyView>
                        <VerticalStackLayout VerticalOptions="Center" Spacing="10" Padding="20">
                            <Label Text="文" FontSize="40" TextColor="{StaticResource TextMuted}" HorizontalTextAlignment="Center"/>
                            <Label Text="이 카테고리에는 문법 카드가 없어요" Style="{StaticResource SubTitle}" HorizontalTextAlignment="Center"/>
                        </VerticalStackLayout>
                    </CollectionView.EmptyView>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardBorder}" Padding="18" Margin="0,0,0,10">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ShowGrammarDetailCommand}"
                                        CommandParameter="{Binding .}"/>
                                </Border.GestureRecognizers>
                                <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,Auto" RowSpacing="6" ColumnSpacing="12">
                                    <Label Text="{Binding Title}" FontFamily="RobotoSerif" FontSize="20" LineBreakMode="TailTruncation"/>
                                    <Border Grid.Column="1" IsVisible="{Binding HasUserStatus}" VerticalOptions="Center" Padding="9,3" StrokeThickness="0" BackgroundColor="{Binding UserStatus, Converter={StaticResource StatusColor}}" StrokeShape="RoundRectangle 10">
                                        <Label Text="{Binding UserStatus}" FontSize="10" FontAttributes="Bold" TextColor="White"/>
                                    </Border>
                                    <HorizontalStackLayout Grid.Row="1" Spacing="7">
                                        <Label Text="{Binding Category}" Style="{StaticResource MutedLabel}" TextColor="{StaticResource Accent}"/>
                                        <Label Text="· 탭해서 자세히" Style="{StaticResource MutedLabel}"/>
                                    </HorizontalStackLayout>
                                    <Label Grid.Row="1" Grid.Column="1" Text="›" FontSize="24" TextColor="{StaticResource TextMuted}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <Border IsVisible="{Binding HasError}" BackgroundColor="{StaticResource DangerSoft}" StrokeThickness="0" StrokeShape="RoundRectangle 16" Padding="20" VerticalOptions="Start">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="{Binding ErrorMessage}" TextColor="{StaticResource Danger}"/>
                        <Button Text="다시 시도" Command="{Binding LoadGrammarsCommand}" Style="{StaticResource SecondaryButton}"/>
                    </VerticalStackLayout>
                </Border>
            </Grid>
        </Grid>

        <Grid IsVisible="{Binding IsDetailVisible}" BackgroundColor="#D90B1120">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding CloseDetailCommand}"/>
            </Grid.GestureRecognizers>
            <Border Style="{StaticResource CardBorder}"
                    Padding="{OnIdiom Phone=22, Default=28}"
                    Margin="24"
                    WidthRequest="{OnIdiom Phone=330, Default=560}"
                    MaximumHeightRequest="680"
                    VerticalOptions="Center"
                    HorizontalOptions="Center">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer/>
                </Border.GestureRecognizers>
                <ScrollView>
                    <VerticalStackLayout Spacing="16">
                        <Grid ColumnDefinitions="*,Auto">
                            <Label Text="문법 자세히" Style="{StaticResource EyebrowLabel}" TextColor="{StaticResource Accent}" VerticalOptions="Center"/>
                            <Button Grid.Column="1" Text="×" Command="{Binding CloseDetailCommand}" Style="{StaticResource GhostButton}" WidthRequest="42" HeightRequest="42" Padding="0" FontSize="20"/>
                        </Grid>
                        <Label Text="{Binding SelectedGrammar.Title}" FontFamily="RobotoSerif" FontSize="28"/>
                        <Border BackgroundColor="#2A234C" StrokeThickness="0" StrokeShape="RoundRectangle 10" Padding="10,5" HorizontalOptions="Start">
                            <Label Text="{Binding SelectedGrammar.Category}" FontSize="12" FontAttributes="Bold" TextColor="{StaticResource Accent}"/>
                        </Border>
                        <BoxView HeightRequest="1" Color="{StaticResource Stroke}"/>
                        <VerticalStackLayout Spacing="5">
                            <Label Text="핵심 설명" Style="{StaticResource EyebrowLabel}"/>
                            <Label Text="{Binding SelectedGrammar.Explanation}" Style="{StaticResource Body}" TextColor="{StaticResource TextPrimary}" LineBreakMode="WordWrap"/>
                        </VerticalStackLayout>
                        <VerticalStackLayout Spacing="5">
                            <Label Text="예문" Style="{StaticResource EyebrowLabel}" TextColor="{StaticResource Accent}"/>
                            <Label Text="{Binding SelectedGrammar.Example}" FontSize="17" LineBreakMode="WordWrap"/>
                        </VerticalStackLayout>
                        <Border BackgroundColor="{StaticResource SurfaceMuted}" StrokeThickness="0" StrokeShape="RoundRectangle 12" Padding="13">
                            <Label Text="{Binding SelectedGrammar.Note}" Style="{StaticResource MutedLabel}" LineBreakMode="WordWrap"/>
                        </Border>
                        <Button Text="닫기" Command="{Binding CloseDetailCommand}"/>
                    </VerticalStackLayout>
                </ScrollView>
            </Border>
        </Grid>
    </Grid>
</ContentPage>
